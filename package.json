{
  "name": "pw-scraper-with-lambda",
  "version": "1.0.0",
  "description": "Playwright scraper demo using aws lambda",
  "scripts": {
    "start": "ts-node -e 'require(\"./index.ts\").main(\"This is test\")'",
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2022 --outfile=dist/index.js"
  },
  "keywords": ["playwright", "scraping", "aws", "lambda", "api-gateway"],
  "author": "Parth Satodiya",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node18": "^18.2.0",
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.4.2",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.18.15",
    "playwright-chromium": "^1.36.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
